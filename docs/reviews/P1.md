# P1 — Ingest review

Status: PASS
Date: 2026-08-30
HEAD: 528476c89c3910f6a7c5e080ed41eb77eb18044f

## Checks

- Media id is `sha256:<digest>`, filename is not identity.
- `ffprobe` JSON is mapped then discarded; `derived/media-probe.json` stores our schema.
- Duplicate bytes skip copy; `transitionProjectState` is a no-op when already `INGESTED`.
- Failed files in a multi-input ingest do not wipe successful records or reset state.
- No transcription, brand, provider, or publish code.

## Carry forward

`classifyMedia` is a cheap heuristic (landscape → screen). That is acceptable for P1. Do not promote it to a semantic fact in P2.
