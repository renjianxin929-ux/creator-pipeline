# P5 — Edit plan + preview engineering review

Status: PASS (engineering)
Date: 2026-08-30
HEAD: 0933ff6fcf0bbc3b0771be39738f91093c9b2603

R2 visual review is **not** implied by this document.

## Checks

- `edit-plan.json` is a strict contract. Clips use `source` xor `source_asset_id`.
- FFmpeg executor and Remotion preview consume the same plan object.
- CLI stops at `creator render preview`. No approve / export / publish commands.
- Remotion uses a local Chrome/Edge path only; no browser profile.
- OpenCut is not a dependency. Live Grok/MiniMax were not added.
- `PREVIEW_READY` has an empty outbound transition table — P6 must open `HUMAN_APPROVED` later.

## Known limits for R2

- Fixture preview (~112KB) only proves the pipe works. It does not prove daily-use quality.
- Planner v0 is rule-based (keep speech, drop long silence).
- 9:16 only in the preview renderer.
