# P4 — Provider protocol review

Status: PASS
Date: 2026-08-30
HEAD: eebc09d077afe1599369f15869584b7cb76ae1b1

## Checks

- Provider ids are `grok_ui`, `grok_api`, `minimax_api`, `omni_ui`, `manual`. Comments treat billing as separate.
- `grok_ui` is assisted drop-folder, cash estimate 0, `subscription_quota_used=true`. It does not read `XAI_API_KEY`.
- `grok_api` / `minimax_api` stay offline unless key + `RUN_LIVE=1`. P4 submit still sends no HTTP.
- `omni_ui` collect sets `final_eligible: false` and `has_watermark: true`.
- Router estimates before route; over-budget returns `WAITING_USER_ACTION`.
- No Remotion / preview render in this batch.

## Carry forward

Live API transport is intentionally unimplemented. Do not sneak it into P5.
