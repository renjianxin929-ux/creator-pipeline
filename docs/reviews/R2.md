# R2 — First real preview review

Status: WAITING_FOUNDER
Blocked on: Founder watching `render/preview.mp4` from a real Cap / talking-head recording

Do not start P6 until this file is flipped to PASS by Founder.

## How to produce the review artifact

```bash
git pull origin main
creator init r2-real
creator ingest r2-real <your-cap-export>
CREATOR_TRANSCRIBE_ADAPTER=fake creator transcribe r2-real
# If you have FunClip + CREATOR_FUNCLIP_MODEL, omit the fake env instead.
creator edit plan r2-real
creator render preview r2-real
creator status r2-real
```

Open:

```text
workspace/projects/r2-real/render/preview.mp4
```

Also skim `plans/edit-plan.json` to see what the machine kept or dropped.

## Founder questions (constitution §P5)

Answer yes/no + one line each:

1. Does the rough cut actually save time vs hand-editing?
2. Are captions usable (timing + wording)?
3. Is screen-demo zoom natural if present?
4. Is brand packaging consistent enough for daily use?
5. Do any generated clips feel wrong? (none is an acceptable answer)
6. Is this faster than the current manual path?

If 1 or 6 is no, do not open publish automation. Adjust planner/render, rerun preview, then re-ask.
