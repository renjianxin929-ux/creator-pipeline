# P2 — Transcription review

Status: PASS
Date: 2026-08-30
HEAD: fa083d4055f87f7b045887605f1ed68f623612f1

## Checks

- Transcript / SRT / silence-map are our schemas. FunClip JSON is mapped in memory.
- Fake adapter is explicit (`CREATOR_TRANSCRIBE_ADAPTER=fake`), not a silent production default.
- Missing FunClip/model keeps `INGESTED` and raises `TranscriptionWaitingError`.
- Re-run from `TRANSCRIBED` is allowed. No brand / asset-plan / render / publish code.

## Carry forward

`mapFunClipOutput` currently requires integer timestamps. Live FunASR often emits float seconds. Fix when first real model run fails mapping; do not invent a second transcript format.
