# P6 — Approve + export review

Status: PASS
Date: 2026-08-31
HEAD: 5959bedfaf5c025f12131ea51411f81e022a574c

## Checks

- Approval stores `preview_sha256`. Same bytes are idempotent.
- Stale preview deletes the approval and returns `PREVIEW_READY`.
- Export copies bytes only; master hash must equal approval hash.
- Cover is `cover.TODO`. No publisher, cookie, or platform API.
- `EXPORT_READY` does not yet transition to `PUBLISH_READY`.

## Carry forward

R2 still used fake captions. A published package from `r2-real` would ship fixture text. Live posting stays blocked until Founder says otherwise.
